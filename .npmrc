# Keep the dependency tree strict so the engine package can never accidentally
# rely on a hoisted dependency it does not declare. The verifier's determinism
# guarantee is only as trustworthy as its explicit dependency list.
auto-install-peers=true
strict-peer-dependencies=false
