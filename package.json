{
  "name": "nokiadot",
  "version": "0.0.0",
  "private": true,
  "description": "Nostalgic Snake/Xenzia PWA on Celo with GoodDollar rewards. Deterministic shared engine; chain touched only at settlement.",
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
